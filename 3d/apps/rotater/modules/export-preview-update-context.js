export function buildExportPreviewUpdateContextController({
    force = false,
    nowMs = 0,
    lastUpdateMs = 0,
    intervalMs = 0,
    exportCameraState,
    exportFrameEnabled,
    canvasEl,
    renderer,
    scene,
    sourceCamera,
    previewResources,
    getOrbitFrameState,
    getCropFrameVerticalScale,
    setCameraFromOrbitState,
    getCropFrameRect,
    drawRulerOverlay,
    exportFormatEl,
    exportBgColorEl,
    exportPreviewDprMax,
    isPreviewActive,
    evaluateTimingController,
    getPreviewExportSize,
    applyExportSceneForRender,
    devicePixelRatio,
} = {}) {
    return {
        force,
        nowMs,
        lastUpdateMs,
        intervalMs,
        exportCamDist: exportCameraState?.exportCamDist,
        exportCamElev: exportCameraState?.exportCamElev,
        exportCamZoom: exportCameraState?.exportCamZoom,
        exportFrameEnabled,
        canvasEl,
        renderer,
        scene,
        sourceCamera,
        previewRt: previewResources?.previewRt,
        previewRtWidth: previewResources?.previewRtWidth,
        previewRtHeight: previewResources?.previewRtHeight,
        previewCam: previewResources?.previewCam,
        getOrbitFrameState,
        getCropFrameVerticalScale,
        setCameraFromOrbitState,
        getCropFrameRect,
        drawRulerOverlay,
        exportFormatEl,
        exportBgColorEl,
        exportPreviewDprMax,
        isPreviewActive,
        evaluateTimingController,
        getPreviewExportSize,
        applyExportSceneForRender,
        devicePixelRatio,
    };
}
