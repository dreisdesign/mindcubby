# Etsy API & Vercel Integration — MindCubby

This document captures the Etsy API integration and Vercel deployment details implemented in the MindCubby project. It summarizes the OAuth PKCE flow, server endpoints, cookie strategy, known issues from debugging, and next steps — essentially a write-up of this work and the conversation that produced it.

---

## Quick links
- API handlers
  - [api/auth/etsy/index.js](api/auth/etsy/index.js) — initiate OAuth (PKCE)
  - [api/auth/etsy/callback.js](api/auth/etsy/callback.js) — token exchange and cookies
  - [api/auth/etsy/status.js](api/auth/etsy/status.js) — auth status check
  - [api/etsy/products.js](api/etsy/products.js) — server-side product fetch
- Frontend
  - [etsy-connect.html](etsy-connect.html) — connect UI + Fetch Products button
- Repo config
  - [package.json](package.json) — set `"type": "module"` for ESM on Vercel

---

## Overview

MindCubby implements a server-side Etsy integration using Etsy Open API v3 and OAuth2 (PKCE). The goals were:

- Authorize a user via Etsy OAuth and securely store the access token in an HttpOnly cookie (no client-side token exposure).
- Provide a server endpoint `/api/etsy/products` that returns the authenticated user's shop listings.
- Keep serverless (Vercel) compatibility and ESM module behavior (avoid CJS compilation issues).

Key constraints/decisions:

- Use PKCE (code_verifier / code_challenge). The `code_verifier` is generated by the server and stored temporarily in an HttpOnly cookie during the auth handshake.
- Store the access token in an HttpOnly, Secure, SameSite=Lax cookie (`etsy_token`). Optionally store `etsy_user_id` (HttpOnly) if the token response includes a usable id.
- Perform all Etsy API calls server-side using `Authorization: Bearer <token>`.

---

## Endpoint summary

- `GET /api/auth/etsy` — starts OAuth flow (PKCE). Creates `code_verifier`, sets `etsy_code_verifier` cookie and redirects the user to `https://www.etsy.com/oauth/connect` with `code_challenge`.

- `GET /api/auth/etsy/callback` — Etsy redirects here with `code`. The handler:
  - Reads the `etsy_code_verifier` cookie
  - POSTs to `https://api.etsy.com/v3/public/oauth/token` (form-encoded) with `grant_type=authorization_code`, `client_id`, `client_secret`, `redirect_uri`, `code`, `code_verifier`.
  - Logs the token response (useful for discovering field names)
  - Stores `etsy_token` (HttpOnly cookie) and, when present, `etsy_user_id` (HttpOnly cookie)
  - Clears `etsy_code_verifier`

- `GET /api/auth/etsy/status` — simple status check (returns whether `etsy_token` exists).

- `GET /api/etsy/products` — the primary products endpoint. Behavior:
  - Reads `etsy_token` from cookies (must be present)
  - Prefers `etsy_user_id` cookie (if numeric) to call `/application/users/{id}/shops`
  - Falls back to trying `/application/me` on `openapi.etsy.com` or `api.etsy.com` to discover shops
  - Once `shopId` is found, calls `/application/shops/{shopId}/listings` and returns `results` to the client
  - Adds `x-api-key` header as fallback if `ETSY_API_KEY` and `ETSY_API_SECRET` are set in env

Refer to [api/etsy/products.js](api/etsy/products.js) for exact behavior and logging.

---

## Environment & Vercel setup

Required environment variables (set in Vercel dashboard for production):

- `ETSY_API_KEY` — Etsy app client id (also referenced as `client_id`)
- `ETSY_API_SECRET` — Etsy app client secret (used in token exchange and optional x-api-key fallback)
- Ensure your Etsy app's redirect URI includes: `https://<your-vercel-domain>/api/auth/etsy/callback`

Vercel notes:

- Keep `package.json` with `"type": "module"` to avoid ESM→CJS transforms that break serverless functions.
- Deploys occur automatically on `git push` to the repository.

---

## Cookies and security

- `etsy_code_verifier` — temporary cookie used during PKCE flow; cleared after token exchange. HttpOnly.
- `etsy_token` — access token cookie. HttpOnly; Secure; SameSite=Lax; Max-Age ~3600s (1 hour). Server uses it to call Etsy APIs.
- `etsy_user_id` — optional cookie set when token response includes a usable numeric id; HttpOnly; Secure; SameSite=Lax.

Security considerations:

- Never expose `client_secret` to client-side code.
- Keep cookies HttpOnly to prevent XSS from reading tokens.
- Consider storing long-lived tokens or refresh tokens in an encrypted server-side store for production.

---

## Common errors & debugging notes (from this work)

- ReferenceError: `paths is not defined` — caused by using a block-scoped `paths` variable outside its scope in `products.js`. Fixed in commit history.

- 400: `Expected int value for 'user_id' (got string)` — Etsy expects a numeric `user_id` when calling `/application/users/{id}/shops`. Ensure any stored `etsy_user_id` is an integer (use `parseInt(...)` and store numeric id), or extract numeric id from `tokenData`.

- `404 /oauth/me` on `api.etsy.com` — this host/path is not implemented; avoid `/oauth/me` fallback. Prefer `/application/me` or `/application/users/{id}/shops`.

- `401 Not authenticated` — means no `etsy_token` cookie was present. Ensure the connect flow completed successfully and cookies are set for the app domain.


Useful logs to inspect (Vercel): search for tags printed by the handlers:

- `OAuth tokenData:` — printed in the callback to inspect the raw token response shape
- `[Products] ...` — prints discovery attempts, endpoints tried, and final errors

---

## Failures & Attempts to Fix

- Summary: During development the integration surfaced several runtime failures (500/400/404) and logic bugs. Below is a concise timeline of the failures, root causes identified, debugging steps performed, and fixes applied (with commit references when available).

- 1) Symptom: ESM/CJS compilation warnings on Vercel (functions failing to run).
  - Root cause: Vercel was transforming ESM into CJS; missing project type.
  - Fix: Added `"type": "module"` to [package.json](package.json) so serverless functions run as ESM.
  - Commit: (earlier change in repo; see git history).

- 2) Symptom: Product fetches returning 502/500 and a 500 log with "ReferenceError: paths is not defined".
  - Root cause: A block-scoped `paths` array was referenced outside its scope in `api/etsy/products.js`.
  - Fix: Scoped `triedPaths` computed explicitly for error response; moved path logic into the correct block.
  - Commit: `2c26f51`

- 3) Symptom: 404 from `https://api.etsy.com/v3/oauth/me` during discovery.
  - Root cause: `/oauth/me` is not implemented on that host — it was a noisy fallback.
  - Fix: Removed `/oauth/me` fallback from discovery.
  - Commit: `805fc25`

- 4) Symptom: 400 `Expected int value for 'user_id' (got string)` when calling `/application/users/me/shops`.
  - Root cause: The endpoint expects a numeric `user_id`; code passed a string or the `"me"` literal which Etsy rejected.
  - Attempts:
    - Added server-side discovery logic to probe `openapi.etsy.com` and `api.etsy.com` hosts for candidate endpoints.
    - Added fallback to call `/application/users/{id}/shops` when a numeric `etsy_user_id` is available.
    - Logged raw `tokenData` to discover which field contained a usable numeric id.
  - Fix: Store `etsy_user_id` parsed as integer (now set from `tokenData` when integer), parse before calling `/application/users/{id}/shops`.
  - Commit: `90178c0` (added user-id cookie); follow-ups also applied.

- 5) Symptom: Token exchange failures (wrong path) and other OAuth hiccups.
  - Root cause: Initial incorrect token endpoint path used or missing PKCE params.
  - Fixes:
    - Corrected token endpoint to `https://api.etsy.com/v3/public/oauth/token`.
    - Implemented PKCE code_challenge and code_verifier handling on `api/auth/etsy`.
    - Ensured `redirect_uri`, `client_id`, `client_secret`, and `code_verifier` are sent in the token exchange.

- 6) Symptom: Sensitive token values appearing in logs (`OAuth tokenData:`).
  - Root cause: We logged the entire token response for debugging.
  - Fix: Replaced raw logging with a sanitized log of response keys. Also added `state` verification and removed or redacted sensitive values.
  - Commit: `50c42d0`

- 7) Security hardening added:
  - Added `state` generation and stored `etsy_oauth_state` in an HttpOnly cookie in `api/auth/etsy/index.js`.
  - Implemented state verification in `api/auth/etsy/callback.js` and clear the state and code_verifier cookies after exchange.
  - Parsed and enforced numeric `etsy_user_id` before using it.
  - Commits: `50c42d0` and related earlier changes.

- Debugging notes & lessons learned:
  - Etsy Open API v3 has multiple similar endpoints across `openapi.etsy.com` and `api.etsy.com`; pick the correct host and path for each call.
  - Always parse and validate IDs returned from token responses before using them in API paths.
  - Treat server logs as sensitive — never log tokens or secrets; use `Object.keys()` or masked values for debugging.
  - Gate client actions (disable Fetch) until `/api/auth/etsy/status` confirms authentication to avoid bypassing Connect.

- Remaining items to complete (recommended):
  - Implement a disconnect/revoke endpoint that revokes tokens and clears cookies.
  - Add token refresh logic or move to encrypted server-side token storage for longer sessions.
  - Remove `_raw` from production responses in `api/etsy/products.js` and redact sensitive fields.
  - Optionally add an automated test or smoke test for the OAuth and product-fetch flows.


## Reproduce the flow (manual test)

1. Ensure the Vercel environment variables above are set and `package.json` has `"type": "module"`.
2. Deploy to Vercel (or run `vercel dev` locally).
3. Visit `/etsy-connect.html` on your deployment, click **Connect to Etsy**, and approve the app.
4. Watch Vercel logs for `OAuth tokenData:` to see the token response structure.
5. Back on the connect page, click **Fetch Products** (the client does `fetch('/api/etsy/products', { credentials: 'include' })`).
6. Inspect Vercel logs for `[Products]` entries to diagnose discovery and listing fetch.

---

## Next steps & recommendations

- Gate the UI: disable/hide **Fetch Products** until `/api/auth/etsy/status` returns authenticated to prevent accidental 500/401s.
- Normalize and persist numeric `user_id` from `tokenData` (server-side) to avoid `Expected int value` errors.
- Add token refresh logic or persistent server-side storage for tokens if you need longer sessions.
- Harden error handling in `products.js` to return helpful messages to the client (not raw 500s).
- Optional: store tokens in a small persistent store (encrypted) instead of cookies for multi-device sessions.

---

## Recent commits (high-level)

- `90178c0` — added user-id cookie from token response + preferring cookie-based shop lookup
- `2c26f51` — fixed a ReferenceError in `products.js` (scoping bug)
- `805fc25` — removed unsupported `/oauth/me` fallback to reduce 404 noise

---

Document created by the development assistant during the Etsy integration work.
